import React from "react";
interface IconProps {
  name: string;
  color?: string;
}
export const Icon = (props: IconProps) => {
  switch (props.name) {
    default: {
      return <></>;
    }
  }
};
